[project]
name = "environmental-stac-orchestrator"
version = "0.0.0"
description = "BAS STAC deployment orchestrator"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
docs = [
    "zensical",
]

[tool.uv]
package = false
