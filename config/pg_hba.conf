# Define access to database
# Allow Unix socket connections (i.e. local)
local   all             all                                     scram-sha-256

# Allow TCP connections from anywhere (with scram)
host    all             all             0.0.0.0/0               scram-sha-256

